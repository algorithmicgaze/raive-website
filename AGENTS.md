# raive.school

Eleventy static site for the RAIVE AI Summer School.

- Verify: `npm run build` (must finish without errors; output in `_site/`).
- Branch policy: commit directly on `main`. Base branch: `main`.
- Deploy: pushing to `main` deploys; Cloudflare Pages builds it automatically. Every push to `main` goes live.
